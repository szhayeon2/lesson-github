import { test,expect } from '@playwright/test';
test('온보딩 → 수업 → 회고와 새로고침',async({page})=>{await page.goto('/');await page.getByRole('button',{name:'데모 기록장 시작하기'}).click();await page.getByLabel('표시 이름').fill('테스트 교사');await page.getByRole('button',{name:'내 기록장 열기'}).click();await page.getByRole('button',{name:'수업 기록',exact:true}).click();await page.getByLabel('수업 제목 *').fill('사실과 의견');await page.getByRole('button',{name:'수업 기록 저장하기'}).click();await page.getByRole('button',{name:'수업 후 회고하기'}).click();await page.getByLabel('잘된 점').fill('근거를 함께 설명했다.');await page.getByRole('button',{name:'회고 저장하기'}).click();await expect(page.getByText('근거를 함께 설명했다.',{exact:true})).toBeVisible();await page.reload();await expect(page.getByRole('button',{name:/사실과 의견/})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=window.innerWidth)).toBe(true);});
